databaseChangeLog:
  - changeSet:
      author: roman
      id: create-roles-table
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  autoIncrement: true
                  type: bigint
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false
                    unique: true
